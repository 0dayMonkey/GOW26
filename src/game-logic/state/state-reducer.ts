/**
 * State Reducer — Game Logic Layer
 *
 * Placeholder — sera complété en Phase 3 (Application Layer).
 * Le reducer centralisera les mutations d'état via les Commands.
 *
 * Pour l'instant, les mutations se font directement via les fonctions
 * de game-state.ts, player.ts, building.ts, etc.
 */

export {};
