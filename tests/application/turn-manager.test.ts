/**
 * Tests — TurnManager (placeholder Phase 3)
 */
import { describe, it, expect } from 'vitest';

describe('TurnManager', () => {
  it('placeholder — sera implémenté en Phase 3', () => {
    expect(true).toBe(true);
  });
});